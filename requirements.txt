# Runtime dependencies for the Mersey Tunnels API client.
# curl_cffi reproduces a browser TLS fingerprint, which the site's Cloudflare
# bot management requires. A stock HTTP client gets a 403 challenge page.
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
